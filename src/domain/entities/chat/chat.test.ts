/*import type { IMessage } from "~/domain/entities/message/message.interface";

import { Chat } from "./chat";
import type { IChat } from "./chat.interface";

describe("Cnat Entity", function () {
  let instance: IChat;

  beforeEach(function () {
    const dateMessage: Date = new Date();
    const messages: IMessage[] = [
      {
        content: "test-content",
        date: dateMessage,
        id: "test-id",
        userId: "test-userId",
      },
    ];
    instance = new Chat("test-id", 12345678910, messages);
  });

  it("can be created", function () {
    expect(instance).toMatchSnapshot();
  });
});*/
